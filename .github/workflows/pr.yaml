name: Pull Request

on: [pull_request]

jobs:
  pr-checking:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2
        with:
          fetch-depth: 30
      - name: Commitsar check
        uses: aevea/commitsar@v0.15.0
